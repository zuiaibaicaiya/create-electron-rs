# Rslib project

## Setup

Install the dependencies:

```bash
npm install
```

## Get started

Build the library:

```bash
npm run build
```

Build the library in watch mode:

```bash
npm run dev
```
